# Required for OpenWeather UV/AQI/alerts. Server-only; never exposed to client.
OPENWEATHER_API_KEY=

# Optional. If empty, app falls back to MapLibre + Open-Meteo geocoding.
# Public — restrict by HTTP referrer in Google Cloud Console.
PUBLIC_GOOGLE_MAPS_API_KEY=
