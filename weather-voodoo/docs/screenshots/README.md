# Screenshots

| File | What it shows |
|---|---|
| `route-overview.jpg` | Route tab — header, map of Phi Phi → Ao Nang, and the trip-finder card with "Best across 3 days" plus the per-day breakdown |
| `route-day-today-expanded.png` | Today's forecast table on the Route tab with two 3h slots expanded; hourly score chips visible, conditions in the user's start window are dimmed |
| `route-day-after-expanded.png` | Same layout on the Day-after tab — different conditions, different scores, useful for comparing |
| `fixed-location.png` | Fixed-location tab — inland point selected; auto-fallback to land mode because no marine data; trip-finder shows best window per day |

If you replace any of these, keep the filenames stable (the project README
embeds them by path). Crop the browser chrome before committing, and run
`oxipng -o4 *.png` or convert to WebP for smaller files. Target < 500 KB each.
